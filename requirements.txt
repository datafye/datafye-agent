fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.0.0
claude-agent-sdk>=0.1.0
httpx>=0.27.0

# Multipart form parsing for file uploads (POST /v1/conversations/{id}/files)
python-multipart>=0.0.9

# Encrypted credentials store (credentials.py)
cryptography>=42.0.0
msgpack>=1.0.0

# JWT validation against accounts' JWKS (auth.py)
pyjwt[crypto]>=2.8.0

# Parsing the deployment descriptor YAML for env_status (main.py)
pyyaml>=6.0
